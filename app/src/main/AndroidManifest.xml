<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.irvanfh.praktikum3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuatdiCode"/>
        <activity android:name=".ListArrayActivity"/>
        <activity android:name=".RelativeActivity"/>
        <activity android:name=".ListViewActivity"/>
        <activity android:name=".GridViewActivity"/>
        <activity android:name=".FrmActivity"/>
        <activity android:name=".LinearHorizontalActivity"/>
        <activity android:name=".LinearVerticalActivity"/>
    </application>

</manifest>